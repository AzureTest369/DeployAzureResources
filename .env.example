# GitHub Configuration
# Required: GitHub Personal Access Token with 'repo' and 'workflow' scopes
# Get one from: https://github.com/settings/tokens
PERSONAL_ACCESS_TOKEN=your_github_token_here

# Optional: Override default GitHub repository settings
# GITHUB_OWNER=AzureTest369
# GITHUB_REPO=DeployAzureResources
# WORKFLOW_FILE=deploy-vm.yml
# WORKFLOW_REF=main

# Optional: Server port
# PORT=3000

# Note: Azure credentials (AZURE_CLIENT_ID, AZURE_TENANT_ID, AZURE_SUBSCRIPTION_ID)
# are configured as GitHub Secrets and used by GitHub Actions workflows.
# They are NOT needed for running the web server locally.
