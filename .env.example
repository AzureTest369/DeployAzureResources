# GitHub Configuration
# Required: GitHub Personal Access Token with repo and workflow scopes
GITHUB_TOKEN=your_github_token_here

# Optional: Override defaults if needed
GITHUB_OWNER=AzureTest369
GITHUB_REPO=DeployAzureResources
WORKFLOW_FILE=deploy-vm.yml
WORKFLOW_REF=main

# Optional: Server port (defaults to 3000)
PORT=3000
